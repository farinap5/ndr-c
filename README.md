# Neander Machine

- Compiler
- Assembler
- Virtual Machine

Create folder `comp` before running __make__ file.

Simple execution:

```bash
make run FILE=equation.mth
```

Clear data under `comp/`.

```bash
make clear
```